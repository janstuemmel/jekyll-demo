---
label: istagram
hide_body: true
fields:
- name: images
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 250
    label: image
  - name: url
    type: text
    config:
      required: true
    label: url
  config:
    min: 
    max: 
    labelField: ''
  label: Images
pages:
- _data/gallery.yml
