---
label: Instagram
hide_body: true
fields:
- name: images
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 250
    label: image
  - name: url
    type: text
    config:
      required: true
    label: url
  config:
    min: 
    max: 
    labelField: ''
  label: Images
- name: foo
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: foo
- name: bar
  type: blocks
  label: bar
  template_types: []
  config:
    min: 
    max: 
pages:
- _data/gallery.yml
